{{- if .Values.unmeshedClient.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "linkerd-max-header-repro.fullname" . }}-unmeshed-client
  labels:
    {{- include "linkerd-max-header-repro.labels" . | nindent 4 }}
    app.kubernetes.io/component: unmeshed-client
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "linkerd-max-header-repro.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: unmeshed-client
  template:
    metadata:
      annotations:
        {{- with .Values.unmeshedClient.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "linkerd-max-header-repro.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: unmeshed-client
    spec:
      containers:
        - name: curl
          image: "{{ .Values.unmeshedClient.image.repository }}:{{ .Values.unmeshedClient.image.tag }}"
          imagePullPolicy: {{ .Values.unmeshedClient.image.pullPolicy }}
          command:
            - sh
            - -c
            - |
              set -eu
              target="http://{{ include "linkerd-max-header-repro.fullname" . }}-server:{{ .Values.server.port }}/large"
              while true; do
                echo "=== $(date -u +%Y-%m-%dT%H:%M:%SZ) unmeshed client: large response header control ==="
                curl -sv --max-time {{ .Values.unmeshedClient.timeoutSeconds }} "$target" -o /tmp/large-body -w '\nhttp_code=%{http_code}\n' || true
                cat /tmp/large-body 2>/dev/null || true
                echo
                sleep {{ .Values.unmeshedClient.intervalSeconds }}
              done
          resources:
            {{- toYaml .Values.unmeshedClient.resources | nindent 12 }}
{{- end }}
